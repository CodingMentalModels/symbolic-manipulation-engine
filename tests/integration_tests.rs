use symbolic_manipulation_engine::{
    self,
    cli::{cli::Cli, filesystem::FileSystem},
};

#[test]
fn test_add_algorithm() {
    //    let filesystem = FileSystem::new()
    //    let mut cli = Cli();
}
